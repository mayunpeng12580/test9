<view class="quick-nav">
  <view class='quick-nav-item' wx:for="{{items}}" wx:key="{{index}}" >
      <image src='{{item.image}}' mode='widthFix' ></image>
      <view class='title'>{{item.title}}</view>
  </view>
</view>